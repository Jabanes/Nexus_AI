"""
Shared Integrations Package — Reusable tool implementations.

Each sub-package (mock, google_calendar, monday, etc.) provides BaseTool
subclasses that can be referenced from tenant config.yaml via handler paths.
"""
